'use strict';

SIF.define("javascripts/Template", function(){
  
  function parse(tpl){
    var lines = tpl.split("<%");
    
  }

  function compile(tpl){
    
    
  }


  function template(tpl, data){
    compile(tpl);
  }


  return template;
});